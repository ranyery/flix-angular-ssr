export interface IMovie {
  ranking: number;
  imdbId: string;
  title: string;
  originalTitle: string;
  imdbRating: string;
  genres: string[];
  tmdbId: number;
  overview: string;
  release_date: string;
  runtime: number;
}
